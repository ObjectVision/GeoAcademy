container Geography: Using = "Units"
{
	// reference WMS layer
	#include<ngr_layer.dms>

	unit<fpoint> rdc_meter_base : DialogData = "ngr_layer";
	unit<fpoint> rdc_meter      := range(rdc_meter_base, point(0f, -1000000f), point(1000000f, 1000000f));
}